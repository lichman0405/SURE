# Agent instructions

Read `MASTER_PROMPT.md` and `CLAUDE.md` before changing code.

Use the task ledger under `tasks/` + `progress/`.

SURE checks AI-built projects. Agent self-report is never proof. Runtime execution is not automatically safe. Unknown evidence stays unknown.
