# Versioning

Use Semantic Versioning after the first public usable release.

During bootstrap, workspace version may remain `0.0.0-bootstrap` until the release phase intentionally moves it to `0.1.0-*`.

Protocol/storage schemas have their own explicit version fields. Do not assume package version equals protocol version.
