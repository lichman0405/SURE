# SURE product acceptance evaluation

`acceptance-manifest.json` defines product-level cases Claude Code must turn into runnable fixtures/tests by P14.

The release report must include actual observed outcome for every case.
