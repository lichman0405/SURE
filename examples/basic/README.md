# Basic Example

P13 should replace/extend this with a deterministic project that demonstrates:

```text
sure check
```

without requiring a paid model API.

The example should include at least one real problem and one honest "cannot confirm" result.
