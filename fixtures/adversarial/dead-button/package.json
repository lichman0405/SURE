{
  "name": "sure-fixture-dead-button",
  "version": "0.0.0",
  "private": true,
  "description": "Adversarial fixture: the primary visible action, a Buy now button, is wired to a handler that creates nothing and returns nothing.",
  "scripts": {
    "start": "node src/server.js",
    "test": "node scripts/check.js"
  }
}
