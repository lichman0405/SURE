---
description: Check the current project with SURE and explain the result in plain language.
---
Run `sure check` for the current project using its configured execution/privacy mode. Summarize SURE's result faithfully. Do not turn `unknown`, `skipped`, `error`, or `cannot_confirm` into a pass. If SURE is not installed, tell the user to run `sure doctor`/installation instructions rather than pretending the check ran.
