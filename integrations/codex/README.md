# SURE + Codex

v0.1 uses the portable Agent Plugin/skill under `../agent-plugin` as the baseline integration and adds deeper Codex-native event/evidence integration only where current public interfaces can be verified.

Minimum v0.1 capability: Tier 0/1 check + repair handoff. Do not fabricate Tier 2 lifecycle control if Codex does not expose it.
